---
layout: default
title: "文章分类"
---

<div id="home">
  <h1>文章分类</h1>
  <ul class="posts">
      {% for category in site.categories %}
	      {{ category | first }} &raquo; {{ category | last | size }} </br>

	      {% for post in category.last %}
	      	<li><span>{{ post.date | date_to_string }}</span> &raquo; <a href="{{ post.url }}">{{ post.title }}</a></li>
	      {% endfor %}
      {% endfor %}
  </ul>
</div>